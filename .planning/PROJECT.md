# GRACE-GC

## What This Is

实现《GRACE：ICLR 论文框架 v3》的主方法、核心对照和实验脚本。当前优先把代码完整写好，在本机完成能运行的正确性测试；随后到服务器做最小证伪实验，根据实际结果决定下一步。

## Core Value

用尽可能简单、正确、可运行的代码实现论文方法，让实验尽快回答方法是否值得继续。

## 工作方式：奥卡姆剃刀

2026-09-15 用户明确纠正：代码优先，然后最小证伪；不要加奇怪的门槛影响实验。该要求替代之前六阶段流程、43项形式化需求和待审批路线图。

- 实现以函数、普通数据结构、配置文件和脚本为主；只在确实有两个不同后端时抽取必要接口。
- 规划是简短的工作清单，不设独立“研究合同阶段”。
- 不建额外的审批流、实验准入系统、多维状态机或通用决策器。
- 模型和数据可使用本地路径或公开标识；运行时尽量保存实际 revision/版本，未填可选元数据不阻止试跑。
- 实验统计量按实际样本计算。样本少、结果不好、区间宽时照常输出；不附加最低样本量/置信区间宽度门槛。
- 论文原有的统计定义和目标值保留为分析参数或对照说明，不变成程序的开关。
- 代码问题边实现边修；理论疑点边记录边研究，不要求解决全部理论问题后才能写代码。
- 必要的数学、数值和集成检查随实现进行；无 GPU 的测试在服务器补跑，日志如实说明。

## Requirements

### Validated

- [x] GRACE 全空间 HT、续写分配、tiny LoRA 与 CPU 数学测试（本机 pytest 199 passed / 1 skipped，2026-09-16）。
- [x] reservoir / basis / 预测器 / IPW / 审计梯度。
- [x] 八个方法入口、checkpoint、分布式归约顺序的 CPU 参考。
- [x] 数据分割、DAPO 冲突丢掉、格式 SFT、规则奖励、独立评测、前缀审计、账本、单卡配置与 README 全流程。

GPU 两阶段真实训练与 U6 尚未在服务器验证。没有真实实验结果。

### Active

- [x] 实现 GRACE 全空间无偏梯度补全、续写分配和 CPU 参考测试。
- [x] 实现历史梯度 reservoir、低秩基底、坐标/风险/成本预测器、IPW 与随机审计。
- [x] 接入 HF actor + vLLM 两阶段 rollout、分布式双流更新与保存恢复（代码已写，待 GPU；`n_gpu>1` 拒绝）。
- [x] 实现核心机制对照及 GRPO、GRPO-short，提供数学数据和独立评测入口。
- [x] 实现真实成本记录、前缀审计、指标与图表脚本，提供小规模与完整 Pilot 参数。
- [x] 提供 A100/5090 的运行配置和简短运行说明。
- [ ] 代码完成后在服务器运行最小证伪，输出原始观测、统计量和局限。

### Out of Scope

- 当前本机实际大模型训练：没有条件，先完成代码。
- 大规模正式实验、完整外部基线复现、A1–A14 全矩阵、代码任务与多模型扩展：后置。
- 审批平台、通用实验治理框架、自动 Go/No-Go 系统：无助于首版实现。
- 改写研究目标以获得漂亮结果、把合成数据当实测：不属于有效实现。

## Context

- 实际原稿：仓库根目录 GRACE_ICLR论文框架_v3.md；用户最初给出的嵌套路径不存在。
- 原稿 SHA256：20b111105c42f116dbcc488355744bb72c5a90b08b8fdba333591bd63dc9a3e4。原稿保留不变。
- 核心实现依据：§4–§6、§8、§13 和附录 B/F/G。
- 最近一次推送是 GitHub `master`（712c9e6）。其后本地还有未提交改动，不以该 hash 当作现役工作树。技术笔记供实现查阅，不要求重跑调研。
- 本机 Windows 无 GPU；CPU 测试已跑。服务器按 README 全流程单卡起步。
- 规划硬件为 4×A100，随后两组各 8×RTX5090；现在 `n_gpu>1` 不能启动。
- GPU-hours 按硬件分别记录，不把5090时间直接叫作 A100-hours。

## 必须实现正确的数学内容

原始目标 G=(R-b)∇θΣ logπ，θ 覆盖全部 q/v LoRA A/B。预测 m=Uf，续写 Z~Bernoulli(p)，估计量 Ghat=m+Z/p·(G-m)。每批分母为预先确定的起步数 N。

预测器/基底/基线/actor 在当前批保持不变；选择 RNG 与后缀和审计分开。风险包含全空间残差和实际 Gram 项。标准梯度下降优化器使用负 Ghat。机制对照共享该目标，GRPO 保留其独立目标。

这些内容决定是否实现了 GRACE，保留相应测试。输入文件不可读、概率非法、维度不匹配等实际错误需清楚报错；论文的效果目标不参与这些检查。

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| 先完整写代码，再做最小证伪 | 用户最新明确要求 | 已采用 |
| 取消额外流程和实验准入门槛 | 奥卡姆剃刀，避免影响实验 | 已采用 |
| 本地直接实现/测试/提交，不逐步请求批准 | 用户要求减少流程干扰 | 已采用 |
| 核心方法和对照范围保持 | 精简工程复杂度，保留研究功能 | 已采用 |
| 理论问题放入实现笔记，实际概率数值计算可独立工作 | 不以重推理论作为代码前置条件 | 已采用 |
| 必要配置、结果、seed、版本随运行保存 | 足以复现，减少手工登记负担 | 已采用 |

## Evolution

随代码更新勾选功能、记录重要方法偏离与已跑测试。已有论文与技术笔记保持可查，实际结果出来后再扩展实验计划。

---
*Last updated: 2026-09-16 after format warmup, DAPO conflict drop, reward/heartbeat health fields, and README full-flow were aligned*
