# Single actor + colocated vLLM. Multi-card rollout uses a100_rollout_4.yaml.
hardware:
  name: a100
  n_gpu: 1
  gpu_memory_gb: 80
vllm:
  tensor_parallel: 1
  dtype: bfloat16
  gpu_memory_utilization: 0.5
backend: gpu_verl
