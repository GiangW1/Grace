# First runnable GPU config. n_gpu>1 is rejected until allreduce is wired.
hardware:
  name: a100
  n_gpu: 1
  gpu_memory_gb: 80
vllm:
  tensor_parallel: 1
  dtype: bfloat16
  gpu_memory_utilization: 0.5
backend: gpu_verl
