# One HF actor GPU and three independent single-GPU vLLM workers.
hardware:
  name: a100
  n_gpu: 4
  gpu_memory_gb: 80
rollout:
  workers: 3
vllm:
  tensor_parallel: 1
  gpu_memory_utilization: 0.5
  dtype: bfloat16
backend: gpu_verl
